# Clojure tools.deps compile cache (only created when running the
# local rigchecker servers from ../rigchecker/ for tests.html parity).
.cpcache/

# macOS
.DS_Store

# etc
.playwright-mcp/
.claude/
rsyncer
extras/
tmp/
